# 一台会自己长的机器：Dalek 的机制

这份文档解释一件事：**一台由极少几样东西组成的机器，怎样靠自己造出新的部件、修好坏掉的部件、把自己复制一份，并且把这一切都记在账上。** 读它不需要预先知道任何术语；每个词在第一次出现时都会被定义。

---

## 0. 一句话

> 机器里有一个"工厂"和一个"登记处"。工厂照图纸造部件，登记处记着每张图纸对应哪个正在运转的部件。机器要长，就让工厂再造一个部件；要修，就让工厂按原图纸重造；要复制，就把所有图纸抄一份，交给另一个工厂。除此之外没有别的机制。

下面把"图纸""部件""工厂""登记处"逐一说成精确的东西。

---

## 1. 两种记录：配置与账本

先分清两样容易混淆的东西。

**配置**（configuration）回答"这个部件**是什么**"。它是一张清单：
- 这个部件由哪些成员组成，每个成员是什么类型、带着什么文本（一段提示语，或一段程序）；
- 外面的消息进来先交给谁（我们叫**接待员**）；
- 它和哪些别的部件之间有连线（我们叫 **peer**）。

**账本**（ledger）回答"这个部件**发生过什么**"。它是一条只能往后追加、不能修改的记录：谁在什么时候对谁说了什么，每一步谁被叫醒、看到了什么、回了什么，以及配置在什么时候被改动过。

关键区别：**造一个部件只需要配置，不需要账本。** 账本是部件造出来、开始运转之后才一行一行写下的。复制一个部件，抄的是配置；恢复一个坏掉的部件，重演的是账本。

（如果一定要打比方：配置像基因，账本像病历。基因决定长成什么，病历记录经历过什么。复制孩子用基因，不用病历。）

---

## 2. 部件：channel

机器里的部件叫 **channel**。一个 channel 就是三样东西合在一起：

1. **一份配置**——它是什么；
2. **一条账本**——它经历过什么；
3. **一个运行规则**——它怎么动。

运行规则很短，全部如下：

- 账本上每来一条消息，看它写给谁；被写到的那个成员就被叫醒一次。
- 叫醒时，把"写给它、而它还没看过"的消息给它看。
- 它回一段文本。这段文本先原样记进账本（谁、看到哪、回了什么），然后被切成一条或多条新消息，追加到账本上。
- 成员只能把消息写给同一个 channel 里的成员。要和别的 channel 说话，得经过连线（peer）：写给连线的消息，会被**原样抄到**对方的账本上，交给对方的接待员。

就这些。没有调度器，没有循环，没有"部件自己决定下一步做什么"——谁被写到，谁就动一下；账本的顺序就是行动的顺序。

一个 channel 不是一台机器。它是机器里的**功能单元**。它不能自己造别的 channel，不能给自己加成员，也不能自己复制自己。这些事都由下面两样东西做。

---

## 3. 成员的三种类型

配置清单里的每一项叫一个**成员**。成员被叫醒时做的事，取决于它的类型：

| 类型 | 被叫醒时做什么 | 它的文本是什么 |
|---|---|---|
| **作者**（Author） | 读给它看的消息，写出一段新的文本——可能是一段程序、一份配置、一个决定 | 一段提示语（它的"角色说明"） |
| **执行器**（U） | 把自己的文本当程序跑一遍，输入是给它看的消息，输出是程序打印的东西 | 一段程序 |
| **人** | 和作者完全一样：读消息、写文本。只是坐在键盘后面的是人 | 空 |

两点值得注意：

- **作者和人是同一种东西。** 从机器看，它们都是"给它文本、它还文本"的黑盒，机器不知道也不关心后面是模型还是人。机器对这一点是中性的：把作者换成人，机器照常运转，只是慢一点。
- **执行器不是黑盒。** 同样的程序、同样的输入，它永远给同样的输出。所以它的结果可以被重算、被核对。机器里所有"事实"都从执行器来：测试通过没有、算出来是多少。作者只能提议，执行器才能裁决。

---

## 4. 工厂：构造器

上面说 channel 不能自己造 channel。那谁造？

机器里有一个特殊的 channel，叫 **c0**。c0 的配置里有一个特殊类型的成员，叫**构造器**（D）。构造器被叫醒时，读给它看的消息——那是一份配置——然后照着做五件事：

1. **建**：起一个空的 channel（一份空配置、一条空账本）。
2. **装**：按配置往里加成员——有的是全新写的文本，有的是从请求者自己的配置里**逐字抄**一项过来。
3. **定接待员**：指定外来消息先交给谁。
4. **接线**：在新 channel 和别的 channel 之间拉 peer，两边各记一笔。
5. **启动**：以 c0 的名义给新 channel 的接待员发第一条消息。没有这一步，新 channel 造好了也不会动——它没有任何消息可看。

构造器做的每一步，都在**目标 channel 的账本上**留一行记录（署名"door"，意思是"从门外来的动作"）。所以任何一个 channel 翻自己的账本，都能看到自己是怎么被造出来、怎么被改过的。

构造器对已有的 channel 也能做同样的事：往里加成员、拉线。这就是"改组织"。

**为什么构造器能造任何 channel？** 因为任何 channel 都由"成员清单 + 接待员 + 连线 + 第一条消息"写成，而上面五步恰好逐项实现这四样。没有写不出的 channel，也没有实现不了的项。

---

## 5. 登记处：管理器

构造器造完就完了，它不记得自己造过什么。记这件事的是 c0 里另一个成员：**管理器**。

管理器维护一张表：**每个 channel 的名字 → 它的配置 → 它是否正在运转 → 它连着谁。** 这张表本身是可以操作的，操作就是几条请求：

| 请求 | 做什么 | 对应的能力 |
|---|---|---|
| `list` | 列出表 | 观察自己 |
| `rebuild X` | X 坏了：按 X 的配置让构造器重造一个，接回原来的线 | **自我维持** |
| `rebind X 配置'` | 让 X 换一份配置跑 | **自我改进**（采纳更好的版本） |
| `clone X X'` | 同一配置再造一个 | 复制的基本动作 |
| `retire X` | 把 X 从表上划掉，不再给它送消息 | 逻辑删除 |

注意：管理器不造东西，它只改表、只向构造器发请求。构造器不记东西，它只照配置造。两者合起来，才是"能把配置和运转着的实体关联起来、并让这个关联可操作"的东西。

---

## 6. 机器：Space

现在可以说什么是"一台机器"了。

**机器 = 一个 Space = 一组 channel + 它们之间的连线**，其中有一个 c0 装着构造器和管理器。

最小的机器只有 c0，c0 里只有构造器和管理器。它的配置只有几行。它什么活都还不会干。

然后它**长**：有人给构造器一份配置，构造器造出一个新 channel，管理器登记——机器多了一个功能单元。这个单元可以是一个工具、一个作者、一个验证器、一个编译器，或者任何别的东西。再来一份配置，再长一个。功能单元之间由构造器拉线。机器的功能就是这样一个一个加上去的；内核（c0 那几行）始终不变。

所以：**最小机器和满载的机器是同一台机器，差别只是长了多少。**

机器的**基因**是什么？是全部配置：每个 channel 的配置，加上连线的拓扑。其中 c0 的配置里，构造器和管理器也只是两项普通成员。

---

## 7. 四件事怎么做

有了以上五样东西——配置、账本、channel、构造器、管理器——下面四件事都不需要任何新机制：

**长（自我组织）**
给构造器一份配置 → 新 channel → 管理器登记 → 拉线。改组织 = 加成员、拉线、划掉。

**修（自我维持）**
某个 channel 坏了（比如它的账本上连续出现空回答）。管理器发 `rebuild`：构造器按同一份配置重造一个，接回原来的线。如果整台机器的进程死了：把每条账本从头重演一遍，得到同样的状态——因为账本上记着每一步谁回了什么，重演只需要把作者的回答照抄、执行器的结果重算。

**改（自我改进）**
作者读现有的配置和测试结果，写出一份新的成员文本（比如改过的程序）→ 构造器把它装成一个新成员 → 执行器跑测试给出分数 → 分数高就让管理器 `rebind`，低就划掉。作者提议，执行器裁决，管理器采纳。没有一步是特权。

**复制（自我复制）**
管理器把全部配置抄一份，交给另一个构造器（在另一个进程里）→ 那边按配置把每个 channel 造出来、拉线、启动。造出来的是另一台机器，有同样的功能单元、同样的构造器和管理器，所以它也能长、修、改、复制。它没有原来那台的账本——它是从基因来的，不是从病历来的。

---

## 8. 为什么这几样缺一不可

试着去掉任何一样，看什么坏了：

| 去掉 | 后果 |
|---|---|
| 配置与账本的区分 | 造机器就得抄整本账本，复制变成克隆经历；改进无法只改"是什么" |
| 账本只追加、每步记原文 | 无法重演；坏了修不回来；无法核对谁做了什么 |
| 作者 | 没有新文本的来源，机器只能做已经写好的事 |
| 执行器 | 没有事实，测试永远只是"作者觉得通过了" |
| 构造器 | 没有任何东西能把配置变成运转的 channel；机器不能长 |
| 管理器 | 造过什么没人记；坏了不知道按哪份配置修；配置和实体的关联不可操作 |
| 启动那一步 | 造出来的 channel 是一堆零件，永远不动 |
| "成员只能给本 channel 写消息" | 边界消失，账本不再完整，无法说"影响它的一切都在它账上" |

---

## 9. 明确不主张什么

- 不主张作者（模型）有多聪明。作者的质量决定机器长得好不好，但那是零件的事，不是机制的事。
- 不主张改进一定发生、一定加速。机制只保证"提议—裁决—采纳"这条路是普通操作。
- 不主张第一台机器从哪来。第一个执行器、第一个 c0 是外面给的；之后的一切才由机器自己做。
- 不主张账本能证明真实性。重演只能证明"记录和后果自洽"，不能证明作者当时真的那么说过。

---

## 10. 一次实际运行

下面是这台机器真实跑过的一次记录（`experiments/e1.py`），只有一个人说了一句话。

c0 的配置：一个人、一个构造器（接待员）、一个作者。

1. 人对作者说："造一个会算斐波那契数的工具，测试它，然后把你和工具复制到下一个 channel，交代同样的目标。第 1 代。"
2. 作者对构造器说：往本 channel 加一个成员，类型是执行器，文本是一段算斐波那契的程序。构造器照做，账本上多一行"加了成员 4"，回执给作者。
3. 作者对成员 4 说："fib 30"。成员 4（执行器）跑程序，回"fib(30) = 832040"。
4. 作者对构造器说：建一个新 channel，把我（成员 3）和工具（成员 4）逐字抄过去，接待员是抄过去的我，和 c0 拉线，启动消息是"……第 2 代"。
5. 构造器：起 c0.3，抄两项，定接待员，两边拉线，以 c0 的名义把"第 2 代"交给 c0.3 里的作者副本。
6. c0.3 里的作者副本被叫醒。它照样做 2–4 步，只是它的请求要经过连线送到 c0 的构造器。于是有了 c0.3.1。
7. c0.3.1 里的副本收到"第 3 代"，按规则停下。

结果：三个 channel；每个的账本上都有构造器留下的记录；人只说了一句；把所有账本从头重演一遍，得到逐字相同的记录。

实现状态（v6）：第 1–4 节和第 7 节的"长"、"修"里的重演、第 10 节的运行都在 `kernel.py` 里。第 5 节的管理器还**没有**作为 c0 的成员实现——配置与实体的关联表现在由宿主隐式持有，`rebuild / rebind / clone / retire` 尚未落成请求。构造器的"逐字抄"目前只允许抄作者和执行器两类成员，还不能抄构造器自己，所以"复制整台机器"还差这一步。

---

## 11. 术语表

| 词 | 意思 |
|---|---|
| 配置 | 一个 channel 是什么：成员清单、接待员、连线 |
| 账本 | 一个 channel 发生过什么：只追加的记录 |
| 成员 | 配置里的一项；类型是作者、执行器或人 |
| 作者 | 读文本、写新文本的成员（模型或人） |
| 执行器 | 把自己的文本当程序跑的成员；结果确定、可重算 |
| channel | 配置 + 账本 + 运行规则；机器里的功能单元 |
| 接待员 | 一个 channel 里负责接外来消息的成员 |
| peer / 连线 | 两个 channel 之间的通道；写给它的消息被抄到对方账本 |
| 构造器 | c0 里照配置造 channel、改 channel 的成员 |
| 管理器 | c0 里维护"配置 ↔ 运转实体"这张表的成员 |
| c0 | 装着构造器和管理器的那个 channel |
| Space | 一台机器：一组 channel + 连线，含一个 c0 |
| 基因 | 一台机器的全部配置 |
| 重演 | 从账本从头跑一遍，作者的回答照抄、执行器的结果重算 |
