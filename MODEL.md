# 一台会自己长的机器：Dalek 的机制

这份文档解释一件事：**一台由极少几样东西组成的机器，怎样靠自己造出新的部件、修好坏掉的部件、把自己复制一份，并且把这一切都记在账上。** 读它不需要预先知道任何术语；每个词在第一次出现时都会被定义。

---

## 0. 一句话

> 机器的底层有几条"系统调用"：建一个部件、往部件里装一个成员、把两个部件接上、启动一个部件。只有一个叫 c0 的部件能发这些调用。c0 用它们造出的第一个东西是"登记处"，登记处记着每张图纸对应哪个正在运转的部件。此后机器要长，登记处就请 c0 再造一个部件；要修，就请 c0 按原图纸重造；要复制，就把所有图纸抄一份，交给另一台机器的 c0。除此之外没有别的机制。

下面把"系统调用""图纸""部件""登记处"逐一说成精确的东西。

---

## 1. 两种记录：配置与账本

先分清两样容易混淆的东西。

**配置**（configuration）回答"这个部件**是什么**"。它是一张清单：
- 这个部件由哪些成员组成，每个成员是什么类型、带着什么文本（一段提示语，或一段程序）；
- 外面的消息进来先交给谁（我们叫**接待员**）；
- 它和哪些别的部件之间有连线（我们叫 **peer**）。

**账本**（ledger）回答"这个部件**发生过什么**"。它是一条只能往后追加、不能修改的记录：谁在什么时候对谁说了什么，每一步谁被叫醒、看到了什么、回了什么，以及配置在什么时候被改动过。

关键区别：**造一个部件只需要配置，不需要账本。** 账本是部件造出来、开始运转之后才一行一行写下的。复制一个部件，抄的是配置；恢复一个坏掉的部件，重演的是账本。

（如果一定要打比方：配置像基因，账本像病历。基因决定长成什么，病历记录经历过什么。复制孩子用基因，不用病历。）

---

## 2. 部件：channel

机器里的部件叫 **channel**。一个 channel 就是三样东西合在一起：

1. **一份配置**——它是什么；
2. **一条账本**——它经历过什么；
3. **一个运行规则**——它怎么动。

运行规则很短，全部如下：

- 账本上每来一条消息，看它写给谁；被写到的那个成员就被叫醒一次。
- 叫醒时，把"写给它、而它还没看过"的消息给它看。
- 它回一段文本。这段文本先原样记进账本（谁、看到哪、回了什么），然后被切成一条或多条新消息，追加到账本上。
- 成员只能把消息写给同一个 channel 里的成员。要和别的 channel 说话，得经过连线（peer）：写给连线的消息，会被**原样抄到**对方的账本上，交给对方的接待员。

就这些。没有调度器，没有循环，没有"部件自己决定下一步做什么"——谁被写到，谁就动一下；账本的顺序就是行动的顺序。

一个 channel 不是一台机器。它是机器里的**功能单元**。它不能自己造别的 channel，不能给自己加成员，也不能自己复制自己。这些事都由下面两样东西做。

---

## 3. 成员的三种类型

配置清单里的每一项叫一个**成员**。成员被叫醒时做的事，取决于它的类型：

| 类型 | 被叫醒时做什么 | 它的文本是什么 |
|---|---|---|
| **作者**（Author） | 读给它看的消息，写出一段新的文本——可能是一段程序、一份配置、一个决定 | 一段提示语（它的"角色说明"） |
| **执行器**（U） | 把自己的文本当程序跑一遍，输入是给它看的消息，输出是程序打印的东西 | 一段程序 |
| **人** | 和作者完全一样：读消息、写文本。只是坐在键盘后面的是人 | 空 |

两点值得注意：

- **作者和人是同一种东西。** 从机器看，它们都是"给它文本、它还文本"的黑盒，机器不知道也不关心后面是模型还是人。机器对这一点是中性的：把作者换成人，机器照常运转，只是慢一点。
- **执行器不是黑盒。** 同样的程序、同样的输入，它永远给同样的输出。所以它的结果可以被重算、被核对。机器里所有"事实"都从执行器来：测试通过没有、算出来是多少。作者只能提议，执行器才能裁决。

---

## 4. 系统调用：构造原语

上面说 channel 不能自己造 channel。那谁造？

答案和操作系统一样：**系统调用**（syscall）。普通程序不能自己创建进程、打开文件、建立连接；它向内核发一个调用，内核替它做。这里也是：channel 里的成员只会读文本、写文本；建 channel、装成员、接线、启动，这四件事由**运行时**做，成员只能发调用。

构造原语一共五条，每条是一个原子操作：

| 原语 | 做什么 | 冯诺依曼的对应 |
|---|---|---|
| `born <名字>` | 起一个空 channel：一份空配置、一条空账本 | A：建膜 |
| `add <类型> <文本>` | 往某个 channel 的配置里加一个成员，返回它的地址（序号） | A：构造并绑定地址 |
| `copy <来源地址>` | 把请求者配置里的某一项**逐字抄**进目标 channel（不解释文本的内容） | B：复制 |
| `peer <甲> <乙>` | 在两个 channel 之间拉一条线，两边各记一笔 | C：接线 |
| `start <名字> <文本>` | 以门的名义给某个 channel 的接待员发第一条消息 | C：启动 |

（定接待员是 `add` 的一个附带项：配置里记一下"外来消息交给谁"。）

三条规则：

1. **只有 c0 能发这些调用。** 别的 channel 想造东西，得经连线把请求送到 c0。这就是 c0 的定义：**有系统原语的那个 channel**。
2. **每次调用都在目标 channel 的账本上留一行**，署名 "door"（"从门外来的动作"）。所以任何 channel 翻自己的账本，都能看到自己是怎么被造出来、怎么被改过的。
3. **调用由运行时执行，不由任何成员执行。** 成员是黑盒（作者）或程序（执行器），它们的输出只是文本；把文本变成配置改动的是运行时。

c0 里有一个成员专门做翻译：读写给它的消息（一份配置），把它拆成上面的调用逐条发出，再把回执写给请求者。代码里它叫 `D`。**它不是构造器；构造器是那五条原语。** 把翻译员换掉，原语不变。

**为什么这五条能造任何 channel？** 因为任何 channel 都由"成员清单 + 接待员 + 连线 + 第一条消息"写成，而五条原语恰好逐项实现这四样。没有写不出的 channel，也没有实现不了的项。

---

## 5. 登记处：管理器

原语造完就完了，运行时不记得造过什么。记这件事的是**管理器**。

管理器**不是**内核的一部分，也不是凭空存在的表。它是**用第 4 节的原语、按一份配置造出来的功能单元**，出生方式和任何别的部件一样：`born`、`add`、`peer`、`start`，账本上有记录。它的配置是机器基因里的一项。

它和别的功能单元只有一处不同：它的工作是维护一张表——**每个 channel 的名字 → 它的配置 → 它是否正在运转 → 它连着谁**——并且根据表向 c0 发构造请求。表本身也只是它的文本，改表 = 它写一段新文本。

对表的操作就是几条请求：

| 请求 | 管理器做什么 | 对应的能力 |
|---|---|---|
| `list` | 列出表 | 观察自己 |
| `rebuild X` | X 坏了：把 X 的配置送给 c0 重造一个，接回原来的线，表上换成新实体 | **自我维持** |
| `rebind X 配置'` | 把 X 换成按配置′ 造的新实体 | **自我改进**（采纳更好的版本） |
| `clone X X'` | 同一配置再造一个 | 复制的基本动作 |
| `retire X` | 从表上划掉，不再给它送消息 | 逻辑删除 |

分工：**原语只造，不记；管理器只记、只发请求，不造。** 配置和运转实体之间的关联，就存在管理器的表上；因为表是文本、管理器是普通部件，这个关联是可读可改可替换的。

---

## 6. 机器：Space

现在可以说什么是"一台机器"了。

**机器 = 一个 Space = 一组 channel + 它们之间的连线**，其中有一个 c0 能发系统调用，且有一个由 c0 造出来的管理器。

最小的机器是这样开始的：

1. 外面给一个运行时（它实现那五条原语）和一个 c0——空的，只有门。
2. 外面给 c0 一份管理器的配置。c0 用原语把管理器造出来，接上线，启动。
3. 从此以后，外面不再需要碰内核：要造什么，写配置，交给管理器；管理器请 c0 造。

所以最小机器 = **c0 + 管理器**，基因只有管理器那几行配置。它什么活都还不会干。

然后它**长**：来一份配置，管理器请 c0 造一个新 channel，登记——机器多了一个功能单元。这个单元可以是工具、作者、验证器、编译器，或者任何别的东西。再来一份，再长一个。功能单元之间由原语拉线。机器的功能就是这样一个一个加上去的；原语始终不变，c0 始终不变。

所以：**最小机器和满载的机器是同一台机器，差别只是长了多少。**

机器的**基因**是什么？是全部配置：每个 channel 的配置（含管理器的），加上连线的拓扑。原语不在基因里——它是运行时的一部分，像操作系统内核不在任何程序的源码里一样。

---

## 7. 四件事怎么做

有了以上五样东西——配置、账本、channel、原语、管理器——下面四件事都不需要任何新机制：

**长（自我组织）**
给管理器一份配置 → 管理器请 c0 用原语造 → 新 channel → 登记 → 拉线。改组织 = 加成员、拉线、划掉。

**修（自我维持）**
某个 channel 坏了（比如它的账本上连续出现空回答）。管理器发 `rebuild`：c0 按同一份配置重造一个，接回原来的线。如果整台机器的进程死了：把每条账本从头重演一遍，得到同样的状态——因为账本上记着每一步谁回了什么，重演只需要把作者的回答照抄、执行器的结果重算。

**改（自我改进）**
作者读现有的配置和测试结果，写出一份新的成员文本（比如改过的程序）→ c0 用 `add` 把它装成一个新成员 → 执行器跑测试给出分数 → 分数高就让管理器 `rebind`，低就划掉。作者提议，执行器裁决，管理器采纳。没有一步是特权。

**复制（自我复制）**
管理器把全部配置抄一份，交给另一台机器的 c0（在另一个进程里，有同一套原语）→ 那边先造管理器，再按配置把每个 channel 造出来、拉线、启动。造出来的是另一台机器，有同样的功能单元、同样的管理器，所以它也能长、修、改、复制。它没有原来那台的账本——它是从基因来的，不是从病历来的。

---

## 8. 为什么这几样缺一不可

试着去掉任何一样，看什么坏了：

| 去掉 | 后果 |
|---|---|
| 配置与账本的区分 | 造机器就得抄整本账本，复制变成克隆经历；改进无法只改"是什么" |
| 账本只追加、每步记原文 | 无法重演；坏了修不回来；无法核对谁做了什么 |
| 作者 | 没有新文本的来源，机器只能做已经写好的事 |
| 执行器 | 没有事实，测试永远只是"作者觉得通过了" |
| 五条原语 | 没有任何东西能把配置变成运转的 channel；机器不能长 |
| "只有 c0 能发原语" | 谁都能造，没有一处账本记全构造史；构造不可管理 |
| 管理器 | 造过什么没人记；坏了不知道按哪份配置修；配置和实体的关联不可操作 |
| "管理器也是造出来的" | 管理器成了内核的一部分，不能被复制、替换、重造——机器就复制不全自己 |
| 启动那一步 | 造出来的 channel 是一堆零件，永远不动 |
| "成员只能给本 channel 写消息" | 边界消失，账本不再完整，无法说"影响它的一切都在它账上" |

---

## 9. 明确不主张什么

- 不主张作者（模型）有多聪明。作者的质量决定机器长得好不好，但那是零件的事，不是机制的事。
- 不主张改进一定发生、一定加速。机制只保证"提议—裁决—采纳"这条路是普通操作。
- 不主张第一台机器从哪来。第一个执行器、第一个 c0 是外面给的；之后的一切才由机器自己做。
- 不主张账本能证明真实性。重演只能证明"记录和后果自洽"，不能证明作者当时真的那么说过。

---

## 10. 一次实际运行

下面是这台机器真实跑过的一次记录（`experiments/e1.py`），只有一个人说了一句话。

c0 的配置：一个人、一个翻译员 `D`（接待员，把消息拆成原语）、一个作者。

1. 人对作者说："造一个会算斐波那契数的工具，测试它，然后把你和工具复制到下一个 channel，交代同样的目标。第 1 代。"
2. 作者对 `D` 说：往本 channel 加一个成员，类型是执行器，文本是一段算斐波那契的程序。`D` 发 `add`，账本上多一行 door 记录"加了成员 4"，回执给作者。
3. 作者对成员 4 说："fib 30"。成员 4（执行器）跑程序，回"fib(30) = 832040"。
4. 作者对 `D` 说：建一个新 channel，把我（成员 3）和工具（成员 4）逐字抄过去，接待员是抄过去的我，和 c0 拉线，启动消息是"……第 2 代"。
5. `D` 逐条发原语：`born c0.3`，`copy` 两项，定接待员，`peer`，`start`——以 c0 的名义把"第 2 代"交给 c0.3 里的作者副本。
6. c0.3 里的作者副本被叫醒。它照样做 2–4 步，只是它的请求要经过连线送到 c0——因为只有 c0 能发原语。于是有了 c0.3.1。
7. c0.3.1 里的副本收到"第 3 代"，按规则停下。

结果：三个 channel；每个的账本上都有原语留下的 door 记录；人只说了一句；把所有账本从头重演一遍，得到逐字相同的记录。

实现状态（v6）：第 1–4 节、第 7 节的"长"和"修"里的重演、第 10 节的运行都在 `kernel.py` 里（原语对应 `born` / `conf_add` / `conf_in` / peer 接线 / 门发消息；`D` 是翻译员）。第 5 节的管理器还**没有**实现——配置与实体的关联表现在由宿主隐式持有，`rebuild / rebind / clone / retire` 尚未落成请求；第 6 节"c0 先造管理器"的开机顺序也还没有。`copy` 目前只允许抄作者和执行器两类成员，还不能抄 `D` 和管理器，所以"复制整台机器"还差这一步。

---

## 11. 术语表

| 词 | 意思 |
|---|---|
| 配置 | 一个 channel 是什么：成员清单、接待员、连线 |
| 账本 | 一个 channel 发生过什么：只追加的记录 |
| 成员 | 配置里的一项；类型是作者、执行器或人 |
| 作者 | 读文本、写新文本的成员（模型或人） |
| 执行器 | 把自己的文本当程序跑的成员；结果确定、可重算 |
| channel | 配置 + 账本 + 运行规则；机器里的功能单元 |
| 接待员 | 一个 channel 里负责接外来消息的成员 |
| peer / 连线 | 两个 channel 之间的通道；写给它的消息被抄到对方账本 |
| 原语 / 系统调用 | 运行时提供的五条原子操作：born、add、copy、peer、start；只有 c0 能发 |
| `D` | c0 里的翻译员：把请求消息拆成原语逐条发出 |
| 管理器 | 用原语造出来的功能单元，维护"配置 ↔ 运转实体"的表并向 c0 发构造请求 |
| c0 | 能发系统调用的那个 channel |
| Space | 一台机器：一组 channel + 连线，含一个 c0 |
| 基因 | 一台机器的全部配置（含管理器的配置；不含原语） |
| 重演 | 从账本从头跑一遍，作者的回答照抄、执行器的结果重算 |
