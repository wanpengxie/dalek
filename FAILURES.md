# FAILURES — K 中每条法律的出生证明

方法见 SPEC §8。每条法律进入 K 前必须在此有一条记录。格式：现象 / 触发 / 裂在哪 / 加入的法律 / 加入后。

## F1 — 外生根不请自来
- 起点：v2 演示，K 只派发"被点名"的地址
- 触发：人（外生根）没人点名，永远不被 step；演示一步都走不了
- 裂在哪：账本驱动派发默认"外面的东西不会自己说话"，与"世界 = 接入的根"矛盾
- 加入法律：账本走完时轮询外生根一轮；无人开口即停（`run` 的 idle 规则）
- 加入后：消失

## F2 — replay 照抄记录，查不出对记录本身的篡改
- 起点：v2，replay 对所有根照抄 `#step` 记录
- 触发：把 U 的答案在记录和消息里一起改（832040 → 832041），replay 仍 identical
- 裂在哪："确定的根"被当作 oracle 照抄，确定性没被用上
- 加入法律：确定的根（U、H）在 replay 时重算，只有 L 与外生根照抄（`LIVE`）
- 加入后：消失（tamper A DIVERGED）。仍成立的限制：对 L/X 的记录与后果一致地改，replay 查不出——它检验因果自洽，不检验真实性

## F3 — 折叠时重复执行有副作用的展开
- 起点：v2，`#recipe` 的复印在 `_fold` 里做
- 触发：`load()` 重读账本时把复印又做了一遍：打印重复、replay 发散
- 裂在哪：折叠必须是纯函数；有追加的动作只能发生在写入时
- 加入法律：折叠只改内存；跨界/复印由 door 在 `append` 写入时完成，重读时它们已在带上
- 加入后：消失

## F4 — 成员能说内核的词（codex 指出）
- 起点：v2，`_fold` 只看 body 第一行
- 触发：成员发 `#step actor=L upto=999` 改游标；发 `#genesis` 清空机器；发 `#admit` 造外生根
- 裂在哪：词没有作者——sender 章有了，语义上却没用
- 加入法律：`word_of`：door 说 genesis/admit/decl；K 说 step；成员说 decl/disable/enable；机器（M/P）只说文本。说错话的人，他的话只是文本
- 加入后：消失

## F5 — 复印出来的机器不会动（codex 指出）
- 起点：v2，`#recipe` 只复印零件
- 触发：c1 里所有成员 view 为空，没人点名，永远不迈第一步；"复制构造者"≠"启动构造者"
- 裂在哪：机器的描述不只是零件——还有接线和初始控制状态（1948 的 C）。激活状态是 I_D 的一部分，不能由父代事后免费提供
- 加入法律：`#decl M` 的描述 = part（零件）+ in（接待员 = 边界）+ start（启动消息）；door 在子机器里以父机器根 `P:` 的名义把启动消息发给接待员
- 加入后：消失（E1：c0 → c1 → c2）

## F6 — 复印后描述里的地址失效（codex 指出）
- 起点：v3 设计，成员 id = 宣告它的消息地址
- 触发：`c0/3` 复印到 `c1/3`，描述与启动消息里写的 `c0/3` 指向父机器；等价于装载/链接的 relocation 问题
- 裂在哪：描述要么位置无关，要么复印时有确定的重绑定
- 加入法律：复印时计算 map（源地址 → 子地址），对零件 prefix 与启动消息做文本重绑定；map 写进子机器 genesis 与父机器的 `#created` 回执，两边都在带上
- 加入后：消失。注：位置无关的描述（不硬编码地址，从 view 里读地址）仍是更好的写法——E1 的构造者就是这么做的

## F7 — enable 之后积压永远不补（codex 指出）
- 起点：v2，派发指针一次性前进
- 触发：X 被 disable 期间收到的消息被跳过；`#enable X` 后没有新边，X 永远不被唤醒
- 裂在哪：违反"点名给 enabled 地址的消息终被处理"
- 加入法律：`#enable` 折叠时把该地址 cursor 之后、点名给它的账本位置加入 `pending`，派发先清 pending
- 加入后：消失（T6 覆盖）

## F8 — 坏配方留下半成品子机器（codex 指出）
- 起点：v2，recipe 先写 genesis 再逐项查零件
- 触发：零件地址不存在 → KeyError，带上已留下 recipe + 子 genesis
- 裂在哪：多步写入没有原子性
- 加入法律：`valid_target` 在成员发出 `#decl M` 时先验证全部零件存在且可复印（L/U），否则整条丢弃，不落带
- 加入后：消失

## F9 — 成员看不见带子
- 起点：定义自我维持（检测故障要读带子）与自我改进（变异要读别人的 prefix）
- 触发：view 只给"点名给我的"，没有成员能观测系统或读地址簿；只有 K 看得见
- 裂在哪：两个"自我"都写不出来
- 加入法律：第三个根 `H`——带子的只读投影，问询本身入带（`book | msg | range | steps | tail`），确定、replay 时重算
- 加入后：可写。尚未被实验用到（自我维持/改进的程序还没写）

## F10 — U 的草稿目录跨步存活（codex 指出）
- 起点：v2，每地址一个固定草稿目录
- 触发：同一地址下一步能读到上一步写的文件——K 看不见的持久状态
- 裂在哪：违反"U 除输入输出外无 I/O"
- 加入法律：每步全新临时目录，步后删除
- 加入后：消失

## 尚未有出生证明、仍在词表里的
- `#disable` / `#enable`：E1 不需要；保留是因为"逻辑删除"是替换（不 in-place）所需。等替换实验给它出生证明，否则删。
- 多收件人 `to` 列表：E1 不需要（每条都是单收件人）。等某个实验需要原子扇出。

## F11 — "没人开口"被误判为"没有消息落带"
- 起点：v3，idle 停机条件看"轮询后账本是否多了消息"
- 触发：人的第一句被 locality 丢弃（收件人不存在）→ 没有消息落带 → 机器停机，后面的话再也没机会说
- 裂在哪：开口与落带是两件事；说了错话也是说了
- 加入法律：停机条件 = 一轮轮询里所有外生根的 out 都为空
- 加入后：消失

## F12 — 无状态的工具不知道该回给谁
- 起点：E2，spawner（U）分两步：先问 H 要源码，再起子机器
- 触发：第二步的 view 里只有 H 的回答；工具从 view 里抓"发送者"抓到了 H，报告回给 H，H 答"?"，工具再问……死循环，每圈起一个子进程
- 裂在哪：U 每步全新（F10），跨步的上下文只能在带子上；而 H 的回答没有带回问题
- 加入法律：H 的回答第一行引用问题原文（`> msg c0/1 reply-to c0/2`），问者可把需要带回的上下文写在问题里
- 加入后：消失。一般化：**跨步状态放在消息里，不放在工具里**——这是"U 无状态"的正面用法

## F13 — 源码穿过 render/parse 变了形
- 起点：E2，K 源码经 H 投影 → 消息 → view 进入工具
- 触发：render 给每个换行补缩进，parse 只剥换行不剥空格，末尾多出一个空行；子机器 K sha ≠ 父机器
- 裂在哪：投影不是恒等——文本经带子往返要有确定的编解码
- 加入：工具侧剥掉尾部空行（这次够用）。待定：render 的缩进编码是否该改为可逆的（例如不缩进、用行前缀）
- 加入后：K diff = 0 ✓
