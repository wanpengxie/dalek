# Dalek：定义与讨论

从头开始。旧内核（v2–v6）已删，只保留 git 历史。本文档按讨论进度追加；第 0、1 章是定下来的，第 2 章起是讨论。

---

## 第 0 章 公理与定义

### 0.1 公理

**一台机器是一个 Space，不是一个 channel。channel 是器官。**

机器是被构造、被运行、被复制、被比较的单位。channel 不单独构成机器：它不能自己被 `Ω.run`，不能自己复制，它的描述只是机器描述的一部分。

### 0.2 三个定义

| 词 | 定义 |
|---|---|
| **描述** | 一个配置文件（JSON）。死的。说一台机器**是什么**。 |
| **机器** | 可运行、自包含的源码。死的。自包含 = 除了 Ω 什么都不缺，**包括自己的描述原样在内**。 |
| **运行的机器** | `Ω.run(机器)` 得到的进程，加上它运行后写下的账本。活的。 |

三者的关系：

```
描述 ──construct──▶ 机器 ──Ω.run──▶ 运行的机器
  ▲                   │
  └───── decl ────────┘          （机器里原样带着描述；decl 把它取出来）
```

- 构造（construct）是 Dalek 的事，输入描述，输出机器。
- 运行（run）是 Ω 的事，输入机器，输出进程。
- 两者分开：构造器不起进程，Ω 不读描述。

---

## 第 1 章 Ω：宿主契约

Ω 是机器运行于其上的底层。它是**固定的、有限的、不含任何 Dalek 词汇的**能力清单。Dalek 是运行在 Ω 上的一个软件；Ω 不知道什么是 channel、c0、管理器、Space、复制。

### 1.1 三条能力

| 能力 | 契约 | 一个实例 |
|---|---|---|
| **运行** | `run(源码, 参数) → 进程`；能等它结束、能杀它 | python3 + 进程 |
| **存储** | `read / write / append(路径, 字节)`；append 原子、落盘 | 文件系统 |
| **网络** | `request(url, 字节) → 字节`；`serve(端口, handler)` | http |

"运行"必须含进程：不只是把源码翻成可执行，而是让它成为一个独立活着的东西，并且能停它。没有这条，子代不能独立于父代存活。

"网络"必须含 `serve`，不只 `request`：机器要能被找到——人要跟它说话，另一台机器要给它递描述。单机实验可以用文件代替；LLM 和跨宿主必须网络。

### 1.2 Ω 明确不提供的

- 谁能构造（Dalek 内部的权限，Dalek 自己管）
- 调度、channel 语义、消息格式
- 重演、验证
- 多台机器之间的隔离（靠目录约定）

### 1.3 判据

Ω 的接口表上出现任何一个 Dalek 词，就是作弊——把要证明的能力偷偷放进了宿主。反过来，只要一个底层系统满足 1.1 的三条，它就是一个合法的 Ω；Dalek 的身份不依赖任何特定宿主。

### 1.4 非确定性的来源

运行和存储是确定的。非确定性只从网络进来：LLM 和人。这是账本必须记原文的唯一来源；重演时作者照抄、程序重算。

### 1.5 第一个 Ω

Linux + python3 + 文件系统 + http。

---

## 第 2 章 讨论：构造器

### 2.1 已定

**签名**：`construct(描述) → 机器`。输入一个 JSON，输出一个可运行、自包含的源码包。

**输出是源码不是进程**，四个理由：
1. 把包变成进程是 Ω 的事。构造器输出进程就得内含某个 Ω 的起进程语义，绑死在那个宿主上。字节才能跨宿主：进程传不过网络，包传得过。
2. 冯诺依曼的 A 造出静止的机器，C 才启动。构造器 = A，输出必须是死的。
3. 死的才能比。不动点判据比的是两个包的字节或两份描述；进程没法比。构造器因此是纯函数：同一份描述，永远同一个包。
4. 两次使用要看得见。包里并排放着按描述造出的源码布局（A 的产物）和原样的描述（B 的产物），谁都能检查描述有没有被吞掉。

**构造器生成得很少**：Dalek 的运行时源码是抄进包里的，成员的程序文本是写成文件的，描述是抄的。构造器主要是把描述摊成 Ω 能跑的目录布局。生成得越少，A 越不可能吞掉 B。

**构造器只用 Ω 的存储**。不起进程，不上网。

**构造一台机器 = 依次构造它的器官**。构造器只有一个核心操作：按一个 channel 描述造一个器官。`construct(Space 描述)` = 对描述里的每个 channel 做这一操作，然后按拓扑接线，然后按启动项写好第一条消息。所以：
- 表面上在解决"channel 怎么造、怎么复制"，实际上解决的就是 Space 的构造与复制——两者不是两个问题。
- channel 从不单独被复制。它只在一台机器被构造时作为器官被造出来；"复制一个 channel" = 用同一份 channel 描述再造一个器官，仍然是构造。
- 递归由此免费得到：Space 描述里某一项若本身是一台机器的描述，构造器对它调用自己。

**通用性**：`construct` 对任何合法描述都工作，不只对自己的。这是它和病毒 / quine 的区别——描述被用两次（解释、抄），且能造别人。验收时必须造一台 ≠ 自己的机器。

**复制的不动点**（验收）：
```
dalek1 = Ω.run(dalek0.construct(dalek0.decl()))
dalek1.decl() == dalek0.decl()
dalek0.construct(G′)，G′ ≠ dalek0.decl()，得到另一台机器且它的 decl() == G′
```

### 2.2 待讨论

1. **描述的形状**。一个 JSON 里必须有什么才够构造：成员（类型、文本）、channel 划分、接待员、拓扑、启动顺序、外部能力需求（模型、凭证）、版本？缺哪一项就造不出等价的机器？
2. **包的形状**。一个"可运行、自包含"的目录里必须有什么：入口、运行时源码、成员文本、描述原样、还有什么？
3. ~~构造器住在哪~~ → 定：**构造器和复制器在描述里，是描述里的第一项。** 按冯诺依曼 E = D + I_D，I_D 描述的正是 A+B+C 自己。对应过来：构造器（construct）和复制器（decl）是 c0 的两个普通成员，它们的描述就是它们的源码，与任何工具的程序文本同等地位。构造器造下一台机器时，把这两段源码当成员文本从**描述里**抄进包——不特殊对待，不从自己运行中的代码取。这就是"描述被用两次"落在构造器自己身上。
   dalek0 的基因因此只有：一个 channel `c0`，两个成员（构造器源码、复制器源码），接待员是构造器，无连线，无启动项。第一个验收对象就是它。
4. ~~一个包是一台 Space 还是一个 channel~~ → 由公理定：**一个包 = 一台机器 = 一个 Space**；描述里的多个 channel 都在这一个包里。由定义推出（未单独决定，可推翻）：一次 `Ω.run(机器)` 是一个进程，channel 是进程里的对象；进程之间只有机器与机器的关系，经网络。
5. **启动（C）放哪**。`Ω.run(包)` 之后谁发第一条消息、谁接线、谁撒手。
6. **内部原语还要不要**。旧模型里的 born / add / copy / peer / start 是运行时的 syscall；现在构造器是纯函数、输出是目录，这些词变成了什么？

---

## 第 3 章 里程碑

### M1 · 最小机器：描述并构造自己

**约定**：一台最小的机器 = Space { c0, c1 }，一条连线 c0–c1。

- **c0：造器官。** 输入一份 channel 配置，在本机器内造出任意合法类型的 channel。纯函数，确定。
- **c1：登记。** 当 c0 造/改了 channel，或某个 channel 自发修改自身形态时，c1 保持注册关系不掉——actor 的注册关系、channel 的连线关系。这张关系就是**配置**。每次改动都入账本：c0 做的记在 c0 的账本，channel 自己做的记在该 channel 的账本。配置因此永远能从账本恢复。`decl` = c1 把当前注册关系原样抄出来。
- 通用性落在 c0 的"任意合法类型的 channel"上；任意机器 = 种子 + 任意配置。

**造另一台机器**（种子 + 基因 + 自发育）：
1. c0 写出**种子**：一台只含 c0′、c1′ 的机器的源码——就是 c0 和 c1 的源码原样。这是 c0 唯一写到机器外面的东西（B 抄 I_D）。
2. `Ω.run(种子)`。
3. 把完整配置（c1 的 `decl`）经网络/文件传给种子机器。
4. c0′ 按配置逐个造器官，c1′ 逐个登记。子机器自己长出来。

父机器的 c0 从不伸手进子机器；递完配置后父子之间只剩网络。独立性由此天然成立。

非确定性不在 c0、c1 里；它只从后来长出的作者器官（LLM、人）进来。

种子的基因：
```
{ channels: [ { name: c0, members: [{kind: U, text: <c0 源码>}], receptionist: 1 },
              { name: c1, members: [{kind: U, text: <c1 源码>}], receptionist: 1 } ],
  peers: [[c0, c1]],
  boot: [] }
```

验收：
```
m′ = Ω.run(m.c0.seed())；m′ ← m.c1.decl()
m′.c1.decl() == m.c1.decl()                              复制闭合
m′ ← G′，G′ ≠ m.c1.decl()，则 m′.c1.decl() == G′         是构造器，不是复印机
杀掉 m，m′ 仍能造 m″                                     独立
杀掉 m′ 的进程，只留账本，重起后 c1.decl() 不变          配置可从账本恢复
```

Ω：Linux + python3 + 文件系统（M1 不需要网络）。
