# Dalek：定义与讨论

从头开始。旧内核（v2–v6）已删，只保留 git 历史。本文档按讨论进度追加；第 0、1 章是定下来的，第 2 章起是讨论。

---

## 第 0 章 三个定义

| 词 | 定义 |
|---|---|
| **描述** | 一个配置文件（JSON）。死的。说一台机器**是什么**。 |
| **机器** | 可运行、自包含的源码。死的。自包含 = 除了 Ω 什么都不缺，**包括自己的描述原样在内**。 |
| **运行的机器** | `Ω.run(机器)` 得到的进程，加上它运行后写下的账本。活的。 |

三者的关系：

```
描述 ──construct──▶ 机器 ──Ω.run──▶ 运行的机器
  ▲                   │
  └───── decl ────────┘          （机器里原样带着描述；decl 把它取出来）
```

- 构造（construct）是 Dalek 的事，输入描述，输出机器。
- 运行（run）是 Ω 的事，输入机器，输出进程。
- 两者分开：构造器不起进程，Ω 不读描述。

---

## 第 1 章 Ω：宿主契约

Ω 是机器运行于其上的底层。它是**固定的、有限的、不含任何 Dalek 词汇的**能力清单。Dalek 是运行在 Ω 上的一个软件；Ω 不知道什么是 channel、c0、管理器、Space、复制。

### 1.1 三条能力

| 能力 | 契约 | 一个实例 |
|---|---|---|
| **运行** | `run(源码, 参数) → 进程`；能等它结束、能杀它 | python3 + 进程 |
| **存储** | `read / write / append(路径, 字节)`；append 原子、落盘 | 文件系统 |
| **网络** | `request(url, 字节) → 字节`；`serve(端口, handler)` | http |

"运行"必须含进程：不只是把源码翻成可执行，而是让它成为一个独立活着的东西，并且能停它。没有这条，子代不能独立于父代存活。

"网络"必须含 `serve`，不只 `request`：机器要能被找到——人要跟它说话，另一台机器要给它递描述。单机实验可以用文件代替；LLM 和跨宿主必须网络。

### 1.2 Ω 明确不提供的

- 谁能构造（Dalek 内部的权限，Dalek 自己管）
- 调度、channel 语义、消息格式
- 重演、验证
- 多台机器之间的隔离（靠目录约定）

### 1.3 判据

Ω 的接口表上出现任何一个 Dalek 词，就是作弊——把要证明的能力偷偷放进了宿主。反过来，只要一个底层系统满足 1.1 的三条，它就是一个合法的 Ω；Dalek 的身份不依赖任何特定宿主。

### 1.4 非确定性的来源

运行和存储是确定的。非确定性只从网络进来：LLM 和人。这是账本必须记原文的唯一来源；重演时作者照抄、程序重算。

### 1.5 第一个 Ω

Linux + python3 + 文件系统 + http。

---

## 第 2 章 讨论：构造器

### 2.1 已定

**签名**：`construct(描述) → 机器`。输入一个 JSON，输出一个可运行、自包含的源码包。

**输出是源码不是进程**，四个理由：
1. 把包变成进程是 Ω 的事。构造器输出进程就得内含某个 Ω 的起进程语义，绑死在那个宿主上。字节才能跨宿主：进程传不过网络，包传得过。
2. 冯诺依曼的 A 造出静止的机器，C 才启动。构造器 = A，输出必须是死的。
3. 死的才能比。不动点判据比的是两个包的字节或两份描述；进程没法比。构造器因此是纯函数：同一份描述，永远同一个包。
4. 两次使用要看得见。包里并排放着按描述造出的源码布局（A 的产物）和原样的描述（B 的产物），谁都能检查描述有没有被吞掉。

**构造器生成得很少**：Dalek 的运行时源码是抄进包里的，成员的程序文本是写成文件的，描述是抄的。构造器主要是把描述摊成 Ω 能跑的目录布局。生成得越少，A 越不可能吞掉 B。

**构造器只用 Ω 的存储**。不起进程，不上网。

**通用性**：`construct` 对任何合法描述都工作，不只对自己的。这是它和病毒 / quine 的区别——描述被用两次（解释、抄），且能造别人。验收时必须造一台 ≠ 自己的机器。

**复制的不动点**（验收）：
```
dalek1 = Ω.run(dalek0.construct(dalek0.decl()))
dalek1.decl() == dalek0.decl()
dalek0.construct(G′)，G′ ≠ dalek0.decl()，得到另一台机器且它的 decl() == G′
```

### 2.2 待讨论

1. **描述的形状**。一个 JSON 里必须有什么才够构造：成员（类型、文本）、channel 划分、接待员、拓扑、启动顺序、外部能力需求（模型、凭证）、版本？缺哪一项就造不出等价的机器？
2. **包的形状**。一个"可运行、自包含"的目录里必须有什么：入口、运行时源码、成员文本、描述原样、还有什么？
3. **构造器住在哪**。它是机器源码的一部分（dalek0 里带着 `construct` 和 `decl`），所以 dalek1 也带着。那描述里要不要写"构造器"这一项？如果写，写的是什么——它的源码？
4. **一个包是一台 Space 还是一个 channel**。描述里有多个 channel 时，构造出一个包还是多个包？多个 channel 是一个进程里的多个对象，还是多个进程？
5. **启动（C）放哪**。`Ω.run(包)` 之后谁发第一条消息、谁接线、谁撒手。
6. **内部原语还要不要**。旧模型里的 born / add / copy / peer / start 是运行时的 syscall；现在构造器是纯函数、输出是目录，这些词变成了什么？
