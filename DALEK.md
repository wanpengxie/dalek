# Dalek：定义与讨论

从头开始。旧内核（v2–v6）已删，只保留 git 历史。本文档按讨论进度追加；第 0、1 章是定下来的，第 2 章起是讨论。
codex 审阅（2026-08-29）指出四个阻塞问题；其中 2（两套构造模型并存）、3（c0 ≠ 运行时）已按其意见改入正文。1（运行时归 Ω 还是归包）、4（C 未定义）待议。

---

## 第 0 章 公理与定义

### 0.1 公理

**一台机器是一个 Space，不是一个 channel。channel 是器官。**

机器是被构造、被运行、被复制、被比较的单位。channel 不单独构成机器：它不能自己被 `Ω.run`，不能自己复制，它的描述只是机器描述的一部分。

### 0.2 三个定义

| 词 | 定义 |
|---|---|
| **描述** | 一个配置文件（JSON）。死的。说一台机器**是什么**。 |
| **机器** | 可运行、自包含的源码。死的。自包含 = 除了 Ω 什么都不缺，**包括自己的描述原样在内**。 |
| **运行的机器** | `Ω.run(机器)` 得到的进程，加上它运行后写下的账本。活的。 |

三者的关系：

```
描述 ──pack──▶ 机器 ──Ω.run──▶ 种子进程 ──c0.realize──▶ 运行的机器（完整 Space）
  ▲               │
  └──── decl ─────┘          （机器里原样带着描述；decl 把它取出来）
```

- **pack** 是 Dalek 的事：输入描述，输出机器 = 运行时 + c0、c1 源码 + **描述原样**。死的、确定的、不解释描述。
- **run** 是 Ω 的事：输入机器，输出进程。Ω 不读描述。
- **realize** 是运行中的 c0 的事：读包里的描述，在本机器内逐个造出器官、接线。**解释描述的只有这一步。**
- 只有一条构造路径：种子 + 描述 + 自发育。不存在"在静态目录里先造好全部器官再运行"的路径。

---

## 第 1 章 Ω：宿主契约

Ω 是机器运行于其上的底层。它是**固定的、有限的、不含任何 Dalek 词汇的**能力清单。Dalek 是运行在 Ω 上的一个软件；Ω 不知道什么是 channel、c0、管理器、Space、复制。

### 1.1 三条能力

| 能力 | 契约 | 一个实例 |
|---|---|---|
| **运行** | `run(源码, 参数) → 进程`；能等它结束、能杀它 | python3 + 进程 |
| **存储** | `read / write / append(路径, 字节)`；append 原子、落盘 | 文件系统 |
| **网络** | `request(url, 字节) → 字节`；`serve(端口, handler)` | http |

"运行"必须含进程：不只是把源码翻成可执行，而是让它成为一个独立活着的东西，并且能停它。没有这条，子代不能独立于父代存活。

"网络"必须含 `serve`，不只 `request`：机器要能被找到——人要跟它说话，另一台机器要给它递描述。单机实验可以用文件代替；LLM 和跨宿主必须网络。

### 1.2 Ω 明确不提供的

- 谁能构造（Dalek 内部的权限，Dalek 自己管）
- 调度、channel 语义、消息格式
- 重演、验证
- 多台机器之间的隔离（靠目录约定）

### 1.3 判据

Ω 的接口表上出现任何一个 Dalek 词，就是作弊——把要证明的能力偷偷放进了宿主。反过来，只要一个底层系统满足 1.1 的三条，它就是一个合法的 Ω；Dalek 的身份不依赖任何特定宿主。

### 1.4 非确定性的来源

运行和存储是确定的。非确定性只从网络进来：LLM 和人。这是账本必须记原文的唯一来源；重演时作者照抄、程序重算。

### 1.5 第一个 Ω

Linux + python3 + 文件系统 + http。

---

## 第 1½ 章 物理：最小运行时

### 1.6 冯诺依曼里没有运行时

他的机器是元胞的排列，泡在一个有固定转移规则的空间里。A、B、C 动，不是因为机器里有循环在跑它们，而是空间的规则在更新每个元胞；"伸臂、置态"这些原始动作也是规则自带的。让东西动的机制是**空间的物理**，不在机器里，不在描述里。

对应过来：消息循环、调度、投递、落账（旧称 R）是**物理**，在 Ω 一侧。基因描述组织，不描述物理，正如 I_D 不描述转移规则。

- **Ω = 硬件 + 物理。** 硬件 = 1.1 的三条；物理 = 本章。Atoll 就是这一层。
- **判据照旧**：物理里只能有介质词汇（actor、账本、消息、投递），不能有组织词汇（c0、c1、构造器、登记、复制）。
- **空白宿主**：宿主没装物理时，种子把物理的实现带过去——像把元胞自动机的规则装进新机器。它是世界的一部分，不是基因的一部分。
- **代价**：物理对机器不可改。机器能改的只有组织。想改物理是换世界。

### 1.7 最小运行时 = 两样东西

**（1）账本驱动器。** 账本多一条，投递给相应的 actor。"投递"展开是三件事，全是物理：

| | 定义 |
|---|---|
| **视图** | 那个 actor 看到什么：写给它、且它上次没看过的那些条（每个 actor 一个游标） |
| **调用** | 按它的 kind 把视图交给它。kind 由物理定，数目固定且很少：**程序**（`Ω.run` 子进程，喂视图，收 stdout）、**oracle**（走网络：LLM、人）、**门**（见 (2)） |
| **落账** | 它的回答原样追加；盖一条步记录（谁、看到哪、回了什么）；再拆成新消息追加 |

全局账本还是每 channel 一本，是便利问题——条件：每个 channel 的账本必须能从全局账本完整投影出来。

**（2）连接器（门）。** 是 actor，不是 channel：channel = 账本 + 成员，门没有自己的账本，它是两本账之间的管子。物理提供的动作只有一个：**写给门的消息，被原样抄到对面那本账上；抄过去的那条署名是对面那扇门，收件人是对面的接待员。**

- 一条连线 = 两扇门，一边一扇，互指。
- a 的成员想跟 b 说话，只能写给 a 里的门。b 的成员看到的是"门说了 X"，不知道也不需要知道门后面是谁。
- 门是膜上唯一的口子。膜内成员只能写给本 channel 的地址；外面的东西进来必须先变成账本上的一行。"影响一个 channel 的一切都在它的账本上"由此保证。
- 内外同一种门：对面是本机器另一个 channel，抄账是进程内一次 append；对面是人、LLM、另一台机器，抄账走 Ω 的网络。门的定义不变，只是抄账的路不同。
- 冯诺依曼的"+"（把两个东西接上）在这里有了定义：接上 = 两本账各有一扇门互指。

**kind 一览。** 三种 kind 都是"物理定结构 + 一个 text 参数"，区别只在 text 是什么：

| kind | 物理定的行为 | text |
|---|---|---|
| 程序 | 把视图喂给它、跑、收 stdout | 源码 |
| oracle | 把视图发出去、收回答 | 提示语 / 模型绑定 |
| 门 | 把消息原样抄到对面 | 对面的地址 |

门最退化：text 不是行为、不是内容，是指针。但它是**唯一 text 指向 channel 外面的 kind**——程序和 oracle 的 text 说"我是什么"，门的 text 说"我连着谁"。整台机器的拓扑 = 全部门的 text 的集合。

**路由**：暂不做。每条消息自带合法地址；物理只认两种地址：本 channel 的成员序号、门。通用路由是一个功能性 actor，以后再说。

### 1.8 待定

新 actor 怎么出现（born / add / peer 在物理里是什么）——见第 2 章，与 syscall 一起定。

---

## 第 2 章 讨论：构造器

### 2.1 已定

**两个接口，分工固定**：
```
pack(G)      → P      死的。P = 运行时 + c0、c1 源码 + G 原样。不解释 G。
c0.realize(G)         活的。运行中的 c0 读 G，在本机器内逐个造器官、接线。唯一解释 G 的地方。
```
旧写法 `construct(G) → 完整机器` 撤回：它和"种子自发育"是两套模型，只能留一套。留自发育。

**pack 的输出是源码不是进程**，四个理由：
1. 把包变成进程是 Ω 的事。pack 输出进程就得内含某个 Ω 的起进程语义，绑死在那个宿主上。字节才能跨宿主：进程传不过网络，包传得过。
2. 冯诺依曼的 A 造出静止的机器，C 才启动。输出必须是死的。
3. 死的才能比。不动点判据比的是两个包的字节或两份描述；进程没法比。pack 因此是纯函数：同一份描述，永远同一个包。
4. 两次使用要看得见。包里并排放着运行时 + 种子源码和原样的 G（B 的产物），谁都能检查描述有没有被吞掉。

**pack 不生成任何东西**：运行时源码是抄的，c0、c1 源码是抄的，G 是抄的。它只是把三样东西放进一个 Ω 能跑的目录。抄得越干净，A 越不可能吞掉 B。

**pack 只用 Ω 的存储**。不起进程，不上网。

**realize：造一台机器 = 依次造它的器官**。c0 只有一个核心操作：按一个 channel 描述造一个器官。`realize(G)` = 对 G 里的每个 channel 做这一操作，然后按拓扑接线，然后按启动项写好第一条消息。所以：
- 表面上在解决"channel 怎么造、怎么复制"，实际上解决的就是 Space 的构造与复制——两者不是两个问题。
- channel 从不单独被复制。它只在一台机器被构造时作为器官被造出来；"复制一个 channel" = 用同一份 channel 描述再造一个器官，仍然是构造。
- 递归由此免费得到：Space 描述里某一项若本身是一台机器的描述，c0 对它调用自己。

**描述 = 组织 + 行为，两半不重叠。**
- 组织（JSON）：有哪些 channel、每个里有哪些成员（kind + 指向源码）、接待员、门指向哪。回答"怎么排"。
- 行为（源码）：每个成员做什么。回答"每个元胞是什么"。
源码可以作为 JSON 里的 text 叶子，描述就是一个 JSON；展开成目录只是它的另一种形式。JSON 里不含行为，源码里不含拓扑——这条分界要守住，否则 c1 折叠账本时分不清哪是组织改动。
kind 的取值范围由物理封闭（程序、oracle、门）；开放的只有源码内容。

**新描述从哪来。** 最小机器 {c0, c1} 造不出新 actor，不是格式的限制，是它没有作者：它的描述只能被抄，不能被扩展。L + U 接成一个 channel（L 写、U 跑、L 改，账本驱动器让它们来回）就是 coding agent，记为 **c2**。c2 的产出是描述：新 actor 的源码 + 一小段组织（它是谁的成员、接谁）。c0 装，c1 记，描述从 {c0, c1, c2} 变成 {c0, c1, c2, F}，F 可遗传——E → E_F，变异在机器内部发生。
新东西的**来源**仍在机器外（LLM 是 Ω 的 oracle），但**生产新描述的组织**进了机器里：机器不产生随机性，机器组织随机性。

**划到工程侧的（不是理论模型）**：描述是一个文件还是一个目录；谁有权发形态改动请求、装了算不算采纳；描述里要不要列出外部能力需求（理论只说：造不出来是 Ω 不够，不是描述错）。

**通用性**：`realize` 对任何合法描述都工作，不只对自己的。这是它和病毒 / quine 的区别——描述被用两次（解释、抄），且能造别人。验收时必须造一台 ≠ 自己的机器。

**复制的不动点**（验收）：
```
m′ = Ω.run(pack(m.c1.decl()))；m′.c0.realize(G)
m′.c1.decl() == m.c1.decl()
Ω.run(pack(G′))，G′ ≠ m.c1.decl()，得到另一台机器且它的 decl() == G′
```

### 2.2 待讨论

1. ~~描述的形状~~ → 理论层已定（组织 + 行为，见 2.1）。具体字段与 syscall 一起定。
2. **包的形状**。一个"可运行、自包含"的目录里必须有什么：入口、运行时源码、成员文本、描述原样、还有什么？
3. ~~构造器住在哪~~ → 定：**构造器和复制器在描述里，是描述里的第一项。** 按冯诺依曼 E = D + I_D，I_D 描述的正是 A+B+C 自己。对应过来：构造器（construct）和复制器（decl）是 c0 的两个普通成员，它们的描述就是它们的源码，与任何工具的程序文本同等地位。构造器造下一台机器时，把这两段源码当成员文本从**描述里**抄进包——不特殊对待，不从自己运行中的代码取。这就是"描述被用两次"落在构造器自己身上。
   ~~dalek0 的基因因此只有：一个 channel `c0`，两个成员~~（历史方案，已被 M1 的 {c0, c1} 取代）。
4. ~~一个包是一台 Space 还是一个 channel~~ → 由公理定：**一个包 = 一台机器 = 一个 Space**；描述里的多个 channel 都在这一个包里。由定义推出（未单独决定，可推翻）：一次 `Ω.run(机器)` 是一个进程，channel 是进程里的对象；进程之间只有机器与机器的关系，经网络。
5. **启动（C）放哪**。`Ω.run(包)` 之后谁发第一条消息、谁接线、谁撒手。
6. **内部原语还要不要**。旧模型里的 born / add / copy / peer / start 是运行时的 syscall；现在 pack 是纯函数、realize 在运行中做，这些词变成了什么？

---

## 第 3 章 里程碑

### M1 · 最小机器：描述并构造自己

**约定**：一台最小的机器 = Space { c0, c1 }，一条连线 c0–c1。

- **运行时 ≠ c0。** 运行时（第 1½ 章的物理：账本驱动器、视图、kind、门）驱动所有 channel，**包括 c0**。c0 是被驱动的一个器官，不是驱动者。两者可以写在同一个文件、入口可以从 c0 起，但理论对象分开——否则"c0 是被运行时驱动的 channel，同时又是驱动自己的运行时"。
- **c0：唯一持有形态变更能力的器官。** 输入一份 channel 配置，在本机器内造出任意合法类型的 channel（`realize`）。造器官是确定的。
- **c0 的请求集 = Dalek 的 syscall。** 任何形态改动——不管谁想改、改谁，包括一个 channel 想改自己——都是给 c0 发一条请求，c0 执行、c0 记账。构造权只在 c0。Ω 不认识这些请求；它们是 Dalek 自己的系统调用，Ω 的三条是宿主能力，两层分开。
- **入账规则**：一条形态改动请求的本质是**两边同时记账**——被改的 channel 记一行（它的账本因此自足：影响它的一切都在它账上），c0 记一行（它做了这一步）。
- **c1：登记。** 保持注册关系不掉——actor 的注册关系、channel 的连线关系。这张关系就是**配置**。c1 = **一次折叠 + 保存**：对所有账本折叠一遍得到配置，落成一个文件；`decl` = 把这个文件原样抄出。文件是派生物，不是私有状态：杀掉 c1 重起，重新折一遍得到同一个文件——"配置可从账本恢复"不是纪律，是定义。
- 通用性落在 c0 的"任意合法类型的 channel"上；任意机器 = 种子 + 任意配置。

**造另一台机器**（唯一路径：种子 + 描述 + 自发育）：
1. `G = c1.decl()`：当前可遗传描述，原样。
2. `P = pack(G)`：P = 运行时 + c0、c1 源码 + G 原样。这是唯一写到机器外面的东西（B 抄 I_D）。
3. `Ω.run(P)`：种子进程起来，运行时开始驱动 c0′、c1′。
4. `c0′.realize(G)`：c0′ 读包里的 G，逐个造器官、接线，c1′ 逐个登记。子机器自己长出来。G 里的 `c0`、`c1` 两项与种子逐字核对：相同 → 跳过；不同 → 报错（构造器自身的变异是另一个里程碑）。

父机器的 c0 从不伸手进子机器；P 递出去之后父子之间只剩网络。独立性由此天然成立。
第 2–3 步由谁调用（C）——待定，见 2.2.5。在 C 进入机器之前，不能宣称自我复制闭合。

非确定性不在 c0、c1 里；它只从后来长出的作者器官（LLM、人）进来。

种子的基因：
```
{ channels: [ { name: c0, members: [{kind: U, text: <c0 源码>}], receptionist: 1 },
              { name: c1, members: [{kind: U, text: <c1 源码>}], receptionist: 1 } ],
  peers: [[c0, c1]],
  boot: [] }
```

验收：
```
m′ = Ω.run(pack(m.c1.decl()))；m′.c0.realize(G)
m′.c1.decl() == m.c1.decl()                              复制闭合
Ω.run(pack(G′))，G′ ≠ m.c1.decl()，其 c1.decl() == G′     是构造器，不是复印机
杀掉 m，m′ 仍能造 m″                                     独立
杀掉 m′ 的进程，只留账本，重起后 c1.decl() 不变          配置可从账本恢复
```

Ω：Linux + python3 + 文件系统（M1 不需要网络）。
